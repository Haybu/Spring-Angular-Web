spring:
  application:
    name: flights-service
  cloud:
    config:
      fail-fast: true

#---
#spring:
#  profiles: cloud
#  cloud:
#    config:
#      uri: ${vcap.services.config-server.credentials.uri}
